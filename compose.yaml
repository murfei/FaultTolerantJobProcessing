services:
  postgres:
    image: 'postgres:17'
    container_name: 'job-store'
    restart: 'always'
    environment:
      - 'POSTGRES_DB=mydatabase'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=myuser'
    ports:
      - '5432'
